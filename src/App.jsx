/* 更新版導覽列設定 */

.nav{
  display:flex;
  align-items:center;
  justify-content:flex-end;
  gap:12px;

  color:#1e293b;
  font-size:22px;
  font-weight:800;
  line-height:1.2;

  text-shadow:none;

  flex-wrap:wrap;

  max-width:780px;
  overflow:hidden;
}

.nav a{
  white-space:nowrap;
}

.nav a:hover{
  color:#2563eb;
}
